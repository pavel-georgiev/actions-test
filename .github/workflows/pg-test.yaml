on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build docker image
        run:  |
          echo ${{ toJson(github) }}
          echo ==========
          echo ${{ github.ref }}
          echo ==========
          echo ${{ github.event_name }}
